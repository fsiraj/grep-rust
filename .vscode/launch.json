{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Grep",
      "type": "codelldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/codecrafters-grep",
      "args": ["-E", "a"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build"
    }
  ]
}
